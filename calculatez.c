#include <stdio.h>

int main()
{
    int x = 20, y = 20, z = 30, w = 10;  // Declare and initialize variables x, y, z, w

    float Z = x % y + z / w;  // Perform calculations: (x % y) + (z / w)

    printf("Result of Z is: %f", Z);  // Print the value of Z

    return 0;  // Return 0 to indicate successful execution
}
